"""Shared evaluation harnesses (Chain P and Chain E).

CP-23's ablation lives here rather than beside either owner's models, because
section 35 decisions are only comparable if both owners measure them the same
way.
"""
